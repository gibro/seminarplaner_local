# local_konzeptgenerator

Initial scaffold for global method set governance.

## Next implementation steps
1. Expand `db/install.xml` for all global tables and workflow logs.
2. Implement workflow service (`Draft -> Review -> Published`).
3. Add global set management UI and capability checks.
4. Expose reusable services for `mod_konzeptgenerator`.
